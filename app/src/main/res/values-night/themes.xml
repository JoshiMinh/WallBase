<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Dark variant of the base app theme -->
    <style name="Theme.WallBase" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/black</item>
    </style>

    <!-- Dark splash screen: deep-dark background so the first frame is never white -->
    <style name="Theme.WallBase.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/black</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/splash_icon_background</item>
        <item name="postSplashScreenTheme">@style/Theme.WallBase</item>
    </style>

</resources>
